import { HttpClient } from '@angular/common/http';
import { Injectable, inject } from '@angular/core';
import { AddMapMarker, MapMarkersResponse } from '../models/maps';

@Injectable({
  providedIn: 'root',
})
export class UtilsService {
  constructor() {}
}
